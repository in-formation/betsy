<%= form_with(model: @order_item, url: [@order, @order_item]) do |f|%>
  <div>
    <%= f.label :qty %>
    <%= f.text_field :qty %>
  </div>
  <div>
    <%= f.submit "Submit Qty" %>
  </div>
<% end %>